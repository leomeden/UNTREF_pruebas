///////// Metodos HTTP

    /// GET, POST, PUT, DELETE (y otros)
    // Con al menos estos 4 puedo hacer una API REST


//////// Peticiones HTTP

    //Método
    //URL
    //Encabezado (Header)
    //Cuerpo (Body)


//////// Luego de una solicitud el servidor responde con codigo de estado HTTP

    // 200 (OK)
    // 201 (CREATED)
    // 400 (BAD REQUEST)
    // 401 (UNAUTHORIZED)
    // 403 (FORBIDDEN)
    // 404 (NOT FOUND)
    // 500 (INTERNAL SERVER ERROR)
    // 503 (SERVICE UNAVAILABLE)


///////  Iniciar proyecto node

    // npm init -y (-y para saltear preguntas)

////// reload automatico

    // node --watch .\server.js
